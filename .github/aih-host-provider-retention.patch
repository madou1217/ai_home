diff --git a/package.json b/package.json
--- a/package.json
+++ b/package.json
@@ -52,6 +52,7 @@
     "node-pty": "^1.1.0",
     "open": "^11.0.0",
     "readline-sync": "^1.4.10",
+    "smol-toml": "1.3.1",
     "undici": "^7.22.0",
     "werift": "^0.23.0",
     "ws": "^8.20.0"
diff --git a/lib/account/host-sync.js b/lib/account/host-sync.js
--- a/lib/account/host-sync.js
+++ b/lib/account/host-sync.js
@@ -1,13 +1,12 @@
 'use strict';
 
 const nodePath = require('path');
+const { buildCodexHostProviderBlock } = require('./codex-host-provider');
 const { createCodexNativeCredentialSync } = require('./codex-native-credential-sync');
 const {
   applyCodexStartupPolicy,
   getAihProviderKey,
-  getManagedAihProviderBlock,
   applyManagedAihProviderSection,
-  removeManagedAihProviderSection,
   removeAihManagedCruft,
   syncCodexBuiltinApiBaseUrl,
   hoistModelProviderSections
@@ -199,26 +198,17 @@
       content = `preferred_auth_method = "${apiKeyMode ? 'apikey' : 'oauth'}"\n` + content;
     }
 
-    if (apiKeyMode) {
-      const providerBlock = getManagedAihProviderBlock({
-        openaiBaseUrl: connection.baseUrl,
-        openaiApiKey: connection.apiKey,
-        httpHeaders: connection.httpHeaders,
-        supportsWebsockets: true,
-        authCommand: {
-          command: processObj && processObj.execPath ? processObj.execPath : process.execPath,
-          args: [path.join(__dirname, '..', '..', 'scripts', 'aih-codex-provider-auth.js'), '--gateway', '--ai-home', aiHomeDir]
-        }
-      });
-      content = applyManagedAihProviderSection(content, currentProviderHeader, providerBlock);
-      content = syncCodexBuiltinApiBaseUrl(content, connection.baseUrl);
-    } else {
-      // OAuth must use Codex's native `openai` provider. Remove the previous
-      // AIH provider and its auth command so a stale API-key command cannot be
-      // invoked with an OAuth account that has no OPENAI_API_KEY.
-      content = removeManagedAihProviderSection(content, providerKey);
-      content = syncCodexBuiltinApiBaseUrl(content);
-    }
+    // Default selection and provider registration have different lifetimes.
+    // Keep a callable gateway definition for historical threads in BOTH modes;
+    // --gateway authentication does not require the selected OAuth account to
+    // have OPENAI_API_KEY. Do not inspect token expiry to decide registration.
+    const providerBlock = buildCodexHostProviderBlock(connection, {
+      aiHomeDir,
+      path,
+      nodeExecPath: processObj && processObj.execPath || process.execPath
+    });
+    content = applyManagedAihProviderSection(content, currentProviderHeader, providerBlock);
+    content = syncCodexBuiltinApiBaseUrl(content, apiKeyMode ? connection.baseUrl : undefined);
 
     fs.writeFileSync(configPath, hoistModelProviderSections(content));
   }
diff --git a/lib/server/token-refresh-daemon.js b/lib/server/token-refresh-daemon.js
--- a/lib/server/token-refresh-daemon.js
+++ b/lib/server/token-refresh-daemon.js
@@ -1,6 +1,7 @@
 'use strict';
 
 const crypto = require('node:crypto');
+const { startCodexHostProviderSelfHeal } = require('../account/codex-host-provider');
 const { createCodexNativeCredentialSync } = require('../account/codex-native-credential-sync');
 
 const { refreshCodexAccessToken } = require('./codex-token-refresh');
@@ -405,6 +406,19 @@
     }
   });
 
+  // Host config registration has a lifecycle independent of token validity.
+  // This server-owned loop never selects an account or rewrites auth.json.
+  const hostProviderHeal = startCodexHostProviderSelfHeal({
+    fs, aiHomeDir, hostHomeDir,
+    intervalMs: options.codexHostProviderHealIntervalMs,
+    onRepaired: () => {
+      if (typeof logInfo === 'function') logInfo('codex_host_provider_restored');
+    },
+    onFailure: ({ result }) => {
+      if (typeof logWarn === 'function') logWarn(result && result.reason || 'codex_host_provider_repair_failed');
+    }
+  });
+
   // 启动时立即刷新一次
   tick(true).catch((error) => {
     if (typeof logError === 'function') {
@@ -427,6 +441,7 @@
   return {
     stop: () => {
       nativeCredentialSync.stop();
+      hostProviderHeal.stop();
       clearInterval(timer);
       if (typeof logInfo === 'function') {
         logInfo('Token refresh daemon stopped');
@@ -440,10 +455,12 @@
       totalAuthInvalid,
       refreshIntervalMs,
       skewMs,
-      nativeCredentialSync: nativeCredentialSync.getStats()
+      nativeCredentialSync: nativeCredentialSync.getStats(),
+      hostProviderHeal: hostProviderHeal.getState()
     }),
     forceRefresh: () => {
       nativeCredentialSync.scan();
+      hostProviderHeal.tick();
       return tick(false);
     }
   };
diff --git a/test/host-sync.test.js b/test/host-sync.test.js
--- a/test/host-sync.test.js
+++ b/test/host-sync.test.js
@@ -307,7 +307,7 @@
   assert.doesNotMatch(hostConfig, /aih_10/);
 });
 
-test('host API-key sync pairs native and managed endpoints and removes them on OAuth switch', (t) => {
+test('host API-key sync pairs endpoints and removes only native override on OAuth switch', (t) => {
   const fixture = createFixture(t);
   const custom = registerCodexAccount(fixture, '31', {
     env: { OPENAI_API_KEY: 'custom-test-key', OPENAI_BASE_URL: 'https://custom.example/v1' }
@@ -344,7 +344,7 @@
   assert.doesNotMatch(hostConfig, /^\[model_providers\.aih_20\]$/m);
 });
 
-test('syncGlobalConfigToHost removes stale AIH auth command when switching to OAuth', (t) => {
+test('syncGlobalConfigToHost upgrades stale AIH auth command and retains registration on OAuth', (t) => {
   const fixture = createFixture(t);
   const accountRef = registerCodexAccount(fixture, '21', {
     auth: { tokens: { access_token: 'oauth-access-token' } }
@@ -369,8 +369,11 @@
   const hostConfig = fs.readFileSync(path.join(fixture.hostCodexDir, 'config.toml'), 'utf8');
   assert.match(hostConfig, /^preferred_auth_method = "oauth"$/m);
   assert.match(hostConfig, /^model_provider = "openai"$/m);
-  assert.doesNotMatch(hostConfig, /^\[model_providers\.aih_server\]/m);
-  assert.doesNotMatch(hostConfig, /aih-codex-provider-auth/);
+  assert.match(hostConfig, /^\[model_providers\.aih_server\]/m);
+  assert.match(hostConfig, /aih-codex-provider-auth/);
+  assert.match(hostConfig, /'--gateway'/);
+  assert.doesNotMatch(hostConfig, /env_key|bearer_token/);
+  assert.doesNotMatch(hostConfig, /\/tmp\/aih-codex-provider-auth/);
 });
 
 test('syncGlobalConfigToHost keeps legacy codex hook flag for older codex versions', (t) => {

diff --git a/lib/account/state-service.js b/lib/account/state-service.js
index f10140d..f7dd1b5 100644
--- a/lib/account/state-service.js
+++ b/lib/account/state-service.js
@@ -224,7 +224,7 @@ function createAccountStateService(options = {}) {
       return false;
     }
     if (!canClearRuntimeBlock(p, currentRuntimeState, evidence)) return false;
-    if (evidence === 'credential_update_after_failure'
+    if (p === 'codex' && evidence === 'credential_update_after_failure'
       && deriveAccountRuntimeStatus(currentRuntimeState).status === 'auth_invalid') {
       // New credentials invalidate only evidence against the old credentials.
       // Preserve concurrent rate limits, network failures and per-model state.
diff --git a/test/codex-native-credential-integration.test.js b/test/codex-native-credential-integration.test.js
index 115c6d8..e94b966 100644
--- a/test/codex-native-credential-integration.test.js
+++ b/test/codex-native-credential-integration.test.js
@@ -235,3 +235,23 @@ test('metadata changes during OAuth refresh are merged without losing a rotated
   assert.equal(saved.meta, 'concurrent');
   assert.equal(saved.auth.tokens.refresh_token, 'test-renewed-grant');
 });
+
+
+for (const provider of ['claude', 'grok']) {
+  test(`Codex credential recovery does not change ${provider} runtime cleanup`, t => {
+    const f = fixture(t);
+    const ref = registerAccountIdentity(fs, f.aiHomeDir, {
+      provider, identitySeed: `test-native-recovery-scope:${provider}`
+    }).accountRef;
+    const index = createAccountStateIndex({ fs, aiHomeDir: f.aiHomeDir });
+    const service = createAccountStateService({ fs, accountStateIndex: index });
+    index.upsertRuntimeState(ref, provider, {
+      authInvalidUntil: Date.now() + 100000, lastFailureKind: 'auth_invalid',
+      lastFailureAt: Date.now() - 1000, lastError: 'test-auth-rejection'
+    }, { configured: true, status: 'up', apiKeyMode: false });
+    assert.equal(service.clearRuntimeBlock(ref, provider, {
+      evidence: 'credential_update_after_failure'
+    }), true);
+    assert.equal(service.getAccountState(ref).runtimeState, null);
+  });
+}

'use strict';
// GENERATED BY upstream-endpoints-split script — do not edit manually.
// Behavior-preserving extraction from upstream-endpoints.js.

const { __private: httpUtilsPrivate } = require('./http-utils');

const HOP_BY_HOP_HEADERS = new Set([
  'connection',
  'keep-alive',
  'proxy-authenticate',
  'proxy-authorization',
  'te',
  'trailer',
  'transfer-encoding',
  'upgrade',
  'proxy-connection'
]);
// Streaming generation may legitimately spend well beyond the regular API
// timeout before response headers arrive. This only guards response opening;
// once open, downstream disconnect cancellation owns the stream lifetime.

const STREAM_RESPONSE_OPEN_TIMEOUT_MS = 5 * 60 * 1000;

const isCodeAssistProvider = httpUtilsPrivate && typeof httpUtilsPrivate.isCodeAssistProvider === 'function'
  ? httpUtilsPrivate.isCodeAssistProvider
  : (provider) => provider === 'gemini' || provider === 'agy';


const ANTHROPIC_STREAM_KEEPALIVE_IDLE_MS = 4000;

const ANTHROPIC_STREAM_KEEPALIVE_TICK_MS = 2000;


module.exports = { HOP_BY_HOP_HEADERS, STREAM_RESPONSE_OPEN_TIMEOUT_MS, isCodeAssistProvider, ANTHROPIC_STREAM_KEEPALIVE_IDLE_MS, ANTHROPIC_STREAM_KEEPALIVE_TICK_MS };
